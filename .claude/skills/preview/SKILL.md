---
name: preview
description: 가장 최근에 생성된 뉴스레터 초안을 미리보기로 출력한다.
argument-hint: "[YYYY-MM-DD]"
disable-model-invocation: true
---

# 뉴스레터 미리보기

`$ARGUMENTS`에 날짜가 지정되면 해당 날짜, 없으면 `output/` 디렉토리에서 가장 최근 날짜를 사용한다.

## 실행 절차

1. 대상 날짜 디렉토리(`output/YYYY-MM-DD/`)를 확인한다.
2. 존재하는 파일을 확인한다:
   - `content.md` (마크다운 초안)
   - `email.html` (최종 HTML)
   - `email.txt` (플레인텍스트)
   - `summary.json` (메타 요약)
3. 아래 순서로 내용을 출력한다.

## 출력 순서

### 1. 생성 현황
```
📅 날짜: YYYY-MM-DD
📁 파일 상태:
  content.md  : ✅ 있음 / ❌ 없음
  email.html  : ✅ 있음 / ❌ 없음
  email.txt   : ✅ 있음 / ❌ 없음
  summary.json: ✅ 있음 / ❌ 없음
```

### 2. 수집 통계 (summary.json 있을 경우)
수집 항목 수, 카테고리별 분포를 간략히 출력한다.

### 3. 본문 미리보기 (content.md 또는 email.txt 있을 경우)
전체 내용을 출력한다.

파일이 전혀 없는 경우:
- `/generate-newsletter` 를 먼저 실행하라고 안내한다.
