---
name: search-knowledge
description: knowledge/ 디렉토리에서 키워드로 관련 조사 내용을 검색하고 요약한다. 작업 전 참고 자료를 찾을 때 사용한다.
argument-hint: "[검색 키워드]"
disable-model-invocation: false
allowed-tools: Read, Grep, Glob
---

# 지식 베이스 검색

`$ARGUMENTS`를 키워드로 `knowledge/` 디렉토리에서 관련 내용을 찾는다.

## 실행 절차

1. `knowledge/index.md`를 읽어 파일 목록을 파악한다.
2. `$ARGUMENTS` 키워드가 포함된 파일을 Grep으로 검색한다:
   - 검색 범위: `knowledge/*.md`
   - 대소문자 무시
3. 관련 파일을 읽고 키워드와 관련된 섹션을 추출한다.
4. 아래 형식으로 결과를 출력한다:

```
🔍 검색어: [키워드]

📄 관련 파일: N개
─────────────────────────────
[파일명]: [파일 설명]
  → 관련 내용 요약 (2~3줄)
  → 참고 섹션: [섹션 제목]

─────────────────────────────
💡 다음 단계 제안:
  - 더 자세히 보려면: Read knowledge/[파일명].md
  - 새 조사 저장: /save-research [topic]
```

5. 관련 파일이 없으면 웹 검색 후 `/save-research`로 저장을 권유한다.
